<Window x:Class="MediVault.KeyGen.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MediVault — License Key Generator (Internal)"
        Height="520" Width="640"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI"
        Background="#0F172A">
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="MediVault Key Generator"
                       Foreground="White" FontSize="22" FontWeight="Bold" />
            <TextBlock Text="Internal vendor tool — do not distribute. Mints HMAC-SHA256 signed license keys."
                       Foreground="#94A3B8" FontSize="12" Margin="0,4,0,0" />
        </StackPanel>

        <TextBlock Grid.Row="1" Text="ISSUED TO (optional)" Foreground="#94A3B8" FontSize="10" FontWeight="SemiBold" Margin="0,24,0,6" />
        <TextBox Grid.Row="2"
                 x:Name="IssuedToBox"
                 Padding="10,8"
                 FontSize="13"
                 Background="#1E293B"
                 Foreground="White"
                 BorderBrush="#334155"
                 BorderThickness="1"
                 CaretBrush="White" />

        <Border Grid.Row="3"
                Margin="0,16,0,0"
                Background="#1E293B"
                BorderBrush="#334155"
                BorderThickness="1"
                CornerRadius="8"
                Padding="14">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="GENERATED KEY" Foreground="#94A3B8" FontSize="10" FontWeight="SemiBold" Margin="0,0,0,8" />
                <TextBox Grid.Row="1"
                         x:Name="KeyBox"
                         IsReadOnly="True"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         FontFamily="Consolas"
                         FontSize="12"
                         Background="Transparent"
                         Foreground="#E2E8F0"
                         BorderThickness="0" />
            </Grid>
        </Border>

        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,16,0,0">
            <Button Content="Monthly (30 days)" Click="GenerateMonthly_Click" Style="{DynamicResource Primary}" />
            <Button Content="Yearly (1 year)"   Click="GenerateYearly_Click"  Margin="10,0,0,0" Style="{DynamicResource Primary}" />
            <Button Content="Lifetime"          Click="GenerateLifetime_Click" Margin="10,0,0,0" Style="{DynamicResource Primary}" />
        </StackPanel>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Copy"             Click="Copy_Click"   Style="{DynamicResource Ghost}" />
            <Button Content="Save to file…"    Click="Save_Click"   Style="{DynamicResource Ghost}" Margin="10,0,0,0" />
        </StackPanel>

        <Grid.Resources>
            <Style x:Key="Primary" TargetType="Button">
                <Setter Property="Padding" Value="14,10" />
                <Setter Property="Foreground" Value="#0F172A" />
                <Setter Property="Background" Value="#22D3EE" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="b" CornerRadius="8" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="b" Property="Background" Value="#67E8F9" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style x:Key="Ghost" TargetType="Button">
                <Setter Property="Padding" Value="12,8" />
                <Setter Property="Foreground" Value="#E2E8F0" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="BorderBrush" Value="#334155" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="b" CornerRadius="8" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="b" Property="Background" Value="#1E293B" />
                                    <Setter TargetName="b" Property="BorderBrush" Value="#64748B" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Grid.Resources>
    </Grid>
</Window>
