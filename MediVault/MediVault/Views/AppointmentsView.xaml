<UserControl x:Class="MediVault.Views.AppointmentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:MediVault.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:AppointmentsViewModel}"
             FontFamily="Segoe UI">

    <Grid Margin="32">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="380" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Calendar + actions -->
        <StackPanel Grid.Column="0" Margin="0,0,16,0">
            <Border Style="{StaticResource Card}" Padding="20" Margin="0,0,0,16">
                <StackPanel>
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Calendar" Style="{StaticResource SubheadingText}" />
                            <TextBlock Text="Pick a date to view bookings" Style="{StaticResource MutedText}" />
                        </StackPanel>
                        <Button Grid.Column="1" Content="+ New" Style="{StaticResource PrimaryButton}" Command="{Binding NewCommand}" Padding="12,8" />
                    </Grid>

                    <Calendar SelectedDate="{Binding SelectedDate}" DisplayDate="{Binding SelectedDate}" HorizontalAlignment="Center" />
                </StackPanel>
            </Border>

            <Border Style="{StaticResource Card}" Padding="20">
                <StackPanel>
                    <TextBlock Text="Quick stats" Style="{StaticResource SubheadingText}" Margin="0,0,0,12" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Selected day" Style="{StaticResource MutedText}" />
                            <TextBlock Text="{Binding Appointments.Count}" FontSize="28" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" />
                            <TextBlock Text="appointments" Style="{StaticResource MutedText}" />
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Active doctors" Style="{StaticResource MutedText}" />
                            <TextBlock Text="{Binding Doctors.Count}" FontSize="28" FontWeight="Bold" Foreground="{StaticResource SuccessBrush}" />
                            <TextBlock Text="practitioners" Style="{StaticResource MutedText}" />
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- Right area: list / editor -->
        <Border Grid.Column="1" Style="{StaticResource Card}" Padding="0">
            <Grid>
                <!-- List -->
                <Grid>
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <Setter Property="Visibility" Value="Visible" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsEditing}" Value="True">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <Grid Grid.Row="0" Margin="28,24,28,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="{Binding SelectedDate, StringFormat='{}{0:dddd, MMMM d, yyyy}'}" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" />
                            <TextBlock Style="{StaticResource MutedText}">
                                <Run Text="{Binding Appointments.Count, Mode=OneWay}" />
                                <Run Text=" appointments scheduled" />
                            </TextBlock>
                        </StackPanel>
                        <Button Grid.Column="1" Content="Refresh" Style="{StaticResource SecondaryButton}" Command="{Binding RefreshCommand}" />
                    </Grid>

                    <!-- Day timeline -->
                    <ScrollViewer Grid.Row="1" Padding="28,0,28,24" VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <ItemsControl ItemsSource="{Binding Appointments}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Padding="0" Margin="0,0,0,10" CornerRadius="12" Background="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                                                MouseLeftButtonDown="Appointment_MouseLeftButtonDown"
                                                Tag="{Binding}">
                                            <Border.Effect>
                                                <DropShadowEffect Color="#1E293B" Opacity="0.04" BlurRadius="14" ShadowDepth="2" />
                                            </Border.Effect>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="6" />
                                                    <ColumnDefinition Width="120" />
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <Border Grid.Column="0" Background="{Binding Status, Converter={StaticResource StatusToBrush}}" CornerRadius="12,0,0,12" />
                                                <StackPanel Grid.Column="1" Margin="20,16">
                                                    <TextBlock Text="{Binding StartTime, StringFormat='{}{0:HH:mm}'}" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" />
                                                    <TextBlock Foreground="{StaticResource TextMutedBrush}" FontSize="11">
                                                        <Run Text="to " />
                                                        <Run Text="{Binding EndTime, StringFormat='{}{0:HH:mm}'}" />
                                                    </TextBlock>
                                                </StackPanel>
                                                <StackPanel Grid.Column="2" Margin="16,16" VerticalAlignment="Center">
                                                    <StackPanel Orientation="Horizontal">
                                                        <Border Width="32" Height="32" CornerRadius="16" Background="{Binding Patient.Gender, Converter={StaticResource GenderToBrush}}">
                                                            <TextBlock Text="{Binding Patient.FullName, Converter={StaticResource Initials}}" Foreground="White" FontWeight="Bold" FontSize="11" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                        </Border>
                                                        <StackPanel Margin="10,0,0,0" VerticalAlignment="Center">
                                                            <TextBlock Text="{Binding Patient.FullName}" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" />
                                                            <TextBlock Foreground="{StaticResource TextMutedBrush}" FontSize="11">
                                                                <Run Text="{Binding Doctor.FullName}" />
                                                                <Run Text=" • " />
                                                                <Run Text="{Binding Doctor.Specialty}" />
                                                            </TextBlock>
                                                        </StackPanel>
                                                    </StackPanel>
                                                    <TextBlock Text="{Binding Reason, FallbackValue=''}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,8,0,0" TextWrapping="Wrap" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="3" Margin="16,16" VerticalAlignment="Center">
                                                    <Border Style="{StaticResource Pill}" HorizontalAlignment="Right">
                                                        <Border.Background>
                                                            <SolidColorBrush Color="#EEF4FF" />
                                                        </Border.Background>
                                                        <TextBlock Text="{Binding Status}" Style="{StaticResource PillText}" Foreground="{Binding Status, Converter={StaticResource StatusToBrush}}" />
                                                    </Border>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <Border Padding="40" CornerRadius="12" Background="#F8FAFC" Margin="0,8,0,0">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Appointments.Count}" Value="0">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <StackPanel HorizontalAlignment="Center">
                                    <TextBlock Text="◷" FontSize="32" Foreground="{StaticResource TextMutedBrush}" HorizontalAlignment="Center" />
                                    <TextBlock Text="No appointments on this day" Style="{StaticResource SubheadingText}" HorizontalAlignment="Center" Margin="0,8,0,4" />
                                    <TextBlock Text="Click + New to schedule one." Style="{StaticResource MutedText}" HorizontalAlignment="Center" />
                                </StackPanel>
                            </Border>

                            <Grid Margin="0,16,0,0" Visibility="{Binding HasSelected, Converter={StaticResource BoolToVis}}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="Selected appointment" Style="{StaticResource LabelText}" />
                                    <TextBlock FontWeight="SemiBold">
                                        <Run Text="{Binding Selected.Patient.FullName}" />
                                        <Run Text=" with " Foreground="{StaticResource TextMutedBrush}" />
                                        <Run Text="{Binding Selected.Doctor.FullName}" />
                                    </TextBlock>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Orientation="Horizontal">
                                    <Button Content="Edit" Style="{StaticResource SecondaryButton}" Command="{Binding EditCommand}" Margin="0,0,8,0" />
                                    <Button Content="Delete" Style="{StaticResource DangerButton}" Command="{Binding DeleteCommand}" />
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>

                <!-- Editor -->
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="28">
                    <ScrollViewer.Style>
                        <Style TargetType="ScrollViewer">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsEditing}" Value="True">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ScrollViewer.Style>
                    <StackPanel>
                        <Grid Margin="0,0,0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="Edit appointment" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsNew}" Value="True">
                                                    <Setter Property="Text" Value="New appointment" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <TextBlock Text="Schedule with conflict detection" Style="{StaticResource MutedText}" />
                            </StackPanel>
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <Button Content="Cancel" Style="{StaticResource SecondaryButton}" Command="{Binding CancelCommand}" Margin="0,0,8,0" />
                                <Button Content="Save" Style="{StaticResource PrimaryButton}" Command="{Binding SaveCommand}" />
                            </StackPanel>
                        </Grid>

                        <Border Visibility="{Binding ValidationErrors, Converter={StaticResource StringToVis}}"
                                Background="#FEF2F2" BorderBrush="{StaticResource DangerBrush}" BorderThickness="1"
                                CornerRadius="10" Padding="14" Margin="0,0,0,16">
                            <TextBlock Text="{Binding ValidationErrors}" Foreground="{StaticResource DangerBrush}" TextWrapping="Wrap" />
                        </Border>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,8,12">
                                <TextBlock Text="PATIENT" Style="{StaticResource LabelText}" />
                                <ComboBox ItemsSource="{Binding Patients}" SelectedValue="{Binding Editing.PatientId}" SelectedValuePath="Id" DisplayMemberPath="FullName" />
                            </StackPanel>
                            <StackPanel Grid.Row="0" Grid.Column="1" Margin="8,0,0,12">
                                <TextBlock Text="DOCTOR" Style="{StaticResource LabelText}" />
                                <ComboBox ItemsSource="{Binding Doctors}" SelectedValue="{Binding Editing.DoctorId}" SelectedValuePath="Id" DisplayMemberPath="FullName" />
                            </StackPanel>

                            <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,8,12">
                                <TextBlock Text="DATE" Style="{StaticResource LabelText}" />
                                <DatePicker SelectedDate="{Binding StartDate}" />
                            </StackPanel>
                            <StackPanel Grid.Row="1" Grid.Column="1" Margin="8,0,0,12">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" Margin="0,0,8,0">
                                        <TextBlock Text="START TIME (HH:mm)" Style="{StaticResource LabelText}" />
                                        <TextBox Text="{Binding StartTime, StringFormat='hh\\:mm', UpdateSourceTrigger=LostFocus}" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" Margin="8,0,0,0">
                                        <TextBlock Text="DURATION (min)" Style="{StaticResource LabelText}" />
                                        <TextBox Text="{Binding DurationMinutes, UpdateSourceTrigger=LostFocus}" />
                                    </StackPanel>
                                </Grid>
                            </StackPanel>

                            <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,8,12">
                                <TextBlock Text="REASON" Style="{StaticResource LabelText}" />
                                <TextBox Text="{Binding Editing.Reason, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>
                            <StackPanel Grid.Row="2" Grid.Column="1" Margin="8,0,0,12">
                                <TextBlock Text="STATUS" Style="{StaticResource LabelText}" />
                                <ComboBox ItemsSource="{Binding Statuses}" SelectedItem="{Binding Editing.Status}" />
                            </StackPanel>

                            <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Margin="0,0,0,12">
                                <TextBlock Text="NOTES" Style="{StaticResource LabelText}" />
                                <TextBox Text="{Binding Editing.Notes, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" Height="80" TextWrapping="Wrap" VerticalContentAlignment="Top" />
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>
