<UserControl x:Class="MediVault.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:MediVault.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:DashboardViewModel}"
             FontFamily="Segoe UI">

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="32">
        <StackPanel>
            <!-- Greeting -->
            <Grid Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="{Binding Greeting}" Style="{StaticResource DisplayText}" />
                    <TextBlock Text="{Binding TodayDate}" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0" />
                </StackPanel>
            </Grid>

            <!-- Stat cards -->
            <UniformGrid Columns="4" Margin="0,0,0,24">
                <Border Style="{StaticResource StatCard}" Margin="0,0,16,0">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <Border Width="44" Height="44" CornerRadius="12" Background="#DBE7FE">
                                <TextBlock Text="❤" FontSize="22" Foreground="{StaticResource PrimaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <TextBlock Text="Patients" Style="{StaticResource MutedText}" VerticalAlignment="Center" Margin="14,0,0,0" />
                        </StackPanel>
                        <TextBlock Text="{Binding PatientCount}" FontSize="36" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,16,0,0" />
                        <TextBlock Text="Total registered" Style="{StaticResource MutedText}" />
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource StatCard}" Margin="0,0,16,0">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <Border Width="44" Height="44" CornerRadius="12" Background="#DCFCE7">
                                <TextBlock Text="◷" FontSize="22" Foreground="{StaticResource SuccessBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <TextBlock Text="Today" Style="{StaticResource MutedText}" VerticalAlignment="Center" Margin="14,0,0,0" />
                        </StackPanel>
                        <TextBlock Text="{Binding AppointmentsToday}" FontSize="36" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,16,0,0" />
                        <TextBlock Text="Appointments scheduled" Style="{StaticResource MutedText}" />
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource StatCard}" Margin="0,0,16,0">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <Border Width="44" Height="44" CornerRadius="12" Background="#FEF3C7">
                                <TextBlock Text="℞" FontSize="22" Foreground="{StaticResource WarningBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <TextBlock Text="Prescriptions" Style="{StaticResource MutedText}" VerticalAlignment="Center" Margin="14,0,0,0" />
                        </StackPanel>
                        <TextBlock Text="{Binding ActivePrescriptions}" FontSize="36" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,16,0,0" />
                        <TextBlock Text="Issued in total" Style="{StaticResource MutedText}" />
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource StatCard}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <Border Width="44" Height="44" CornerRadius="12" Background="#F3E8FF">
                                <TextBlock Text="◈" FontSize="22" Foreground="#7C3AED" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <TextBlock Text="Doctors" Style="{StaticResource MutedText}" VerticalAlignment="Center" Margin="14,0,0,0" />
                        </StackPanel>
                        <TextBlock Text="{Binding DoctorsCount}" FontSize="36" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,16,0,0" />
                        <TextBlock Text="Active practitioners" Style="{StaticResource MutedText}" />
                    </StackPanel>
                </Border>
            </UniformGrid>

            <!-- Two-column section -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="1*" />
                </Grid.ColumnDefinitions>

                <!-- Upcoming appointments -->
                <Border Grid.Column="0" Style="{StaticResource Card}" Margin="0,0,16,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0" Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Upcoming appointments" Style="{StaticResource SubheadingText}" />
                                <TextBlock Text="Next appointments scheduled across all doctors" Style="{StaticResource MutedText}" Margin="0,2,0,0" />
                            </StackPanel>
                        </Grid>

                        <ItemsControl Grid.Row="1" ItemsSource="{Binding UpcomingAppointments}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Padding="14" Margin="0,0,0,8" Background="#F8FAFC" CornerRadius="10">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>

                                            <Border Grid.Column="0" Width="48" Height="48" CornerRadius="12" Background="{StaticResource PrimaryGradient}">
                                                <TextBlock Text="{Binding Patient.FullName, Converter={StaticResource Initials}}"
                                                           Foreground="White" FontWeight="Bold" FontSize="14"
                                                           HorizontalAlignment="Center" VerticalAlignment="Center" />
                                            </Border>

                                            <StackPanel Grid.Column="1" Margin="14,0,0,0" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Patient.FullName}" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" />
                                                <TextBlock Foreground="{StaticResource TextMutedBrush}" FontSize="12" Margin="0,2,0,0">
                                                    <Run Text="{Binding Doctor.FullName}" />
                                                    <Run Text=" • " />
                                                    <Run Text="{Binding Reason, FallbackValue='General'}" />
                                                </TextBlock>
                                            </StackPanel>

                                            <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right">
                                                <TextBlock Text="{Binding StartTime, StringFormat='{}{0:MMM d}'}" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" HorizontalAlignment="Right" />
                                                <TextBlock Text="{Binding StartTime, StringFormat='{}{0:HH:mm}'}" Foreground="{StaticResource PrimaryBrush}" FontSize="12" FontWeight="SemiBold" HorizontalAlignment="Right" />
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <TextBlock Grid.Row="1" Text="No upcoming appointments." HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource MutedText}">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding UpcomingAppointments.Count}" Value="0">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>
                </Border>

                <!-- Recent activity -->
                <Border Grid.Column="1" Style="{StaticResource Card}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="0" Margin="0,0,0,12">
                            <TextBlock Text="Recent activity" Style="{StaticResource SubheadingText}" />
                            <TextBlock Text="Audit trail" Style="{StaticResource MutedText}" Margin="0,2,0,0" />
                        </StackPanel>

                        <ItemsControl Grid.Row="1" ItemsSource="{Binding RecentActivity}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,6">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Ellipse Grid.Column="0" Width="10" Height="10" Margin="0,6,12,0" Fill="{Binding Action, Converter={StaticResource StatusToBrush}}" VerticalAlignment="Top" />
                                        <StackPanel Grid.Column="1">
                                            <TextBlock FontSize="12" Foreground="{StaticResource TextPrimaryBrush}">
                                                <Run Text="{Binding Username}" FontWeight="SemiBold" />
                                                <Run Text=" • " Foreground="{StaticResource TextMutedBrush}" />
                                                <Run Text="{Binding Action}" Foreground="{StaticResource TextSecondaryBrush}" />
                                                <Run Text=" " />
                                                <Run Text="{Binding Entity}" Foreground="{StaticResource TextSecondaryBrush}" />
                                            </TextBlock>
                                            <TextBlock Text="{Binding Details}" Foreground="{StaticResource TextMutedBrush}" FontSize="11" TextWrapping="Wrap" Margin="0,2,0,0" />
                                            <TextBlock Text="{Binding Timestamp, StringFormat='{}{0:MMM d HH:mm}'}" Foreground="{StaticResource TextMutedBrush}" FontSize="10" Margin="0,2,0,0" />
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </Border>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</UserControl>
