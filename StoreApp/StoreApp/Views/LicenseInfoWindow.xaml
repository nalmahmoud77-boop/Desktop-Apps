<Window x:Class="StoreApp.Views.LicenseInfoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:StoreApp.Views"
        Title="StoreApp — License" Height="680" Width="640"
        WindowStartupLocation="CenterOwner" WindowStyle="None"
        AllowsTransparency="True" Background="Transparent" ResizeMode="NoResize"
        TextElement.Foreground="{StaticResource TextBrush}">
    <Border CornerRadius="20" Background="{StaticResource BgBrush}">
        <Border.Effect>
            <DropShadowEffect Color="#000" BlurRadius="40" Opacity="0.18" ShadowDepth="0" />
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Title bar -->
            <Grid Margin="24,16,16,0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="License" FontSize="16" FontWeight="Bold" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Click="Close_Click" Content="✕" FontSize="14" Padding="10,2"
                            Background="Transparent" BorderThickness="0" Cursor="Hand" />
                </StackPanel>
            </Grid>

            <!-- Body -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="24,8,24,24">
                <views:LicenseInfoView />
            </ScrollViewer>
        </Grid>
    </Border>
</Window>
